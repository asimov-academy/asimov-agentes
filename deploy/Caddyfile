{
	email {$EMAIL_SSL}
}

{$SUBDOMINIO_BOT} {
	# Só webhooks e health ficam públicos. /admin responde apenas em 127.0.0.1:8000.
	@publico path /health /webhook/*
	handle @publico {
		reverse_proxy api:8000
	}
	handle {
		respond 404
	}
}
